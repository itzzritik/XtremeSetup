---
- vars:
      lxc:
          id: 254
          hostname: jellyfin
          memory: 6144
          swap: 512
          cores: 4
          storage: local-lvm
          ip: '{{ env.JARVIS_NETWORK_PREFIX }}.254'
          gateway: '{{ env.JARVIS_GATEWAY }}'
      paths:
          config: '{{ env.JARVIS_CONFIGS_DIR }}/{{ lxc.hostname }}/etc'
          data: '{{ env.JARVIS_CONFIGS_DIR }}/{{ lxc.hostname }}/lib'
  block:
      - name: Check if Jellyfin LXC exists
        command: pct status {{ lxc.id }}
        register: lxc_status_check
        failed_when: false
        changed_when: false

      - name: Set Jellyfin LXC Exists Fact
        set_fact:
            jellyfin_lxc_exists: '{{ lxc_status_check.rc == 0 }}'

      - name: Create Config Directory on Host
        file:
            path: '{{ item }}'
            state: directory
            mode: '0777'
            recurse: true
        loop:
            - '{{ paths.config }}'
            - '{{ paths.data }}'

      - name: Deploy Jellyfin LXC
        block:
            - name: Update LXC Template Database
              command: pveam update
              changed_when: false

            - name: Fetch Latest Ubuntu LTS Template Name
              shell: pveam available | grep "ubuntu-[0-9.]*-standard" | sort -V | tail -n 1 | awk '{print $2}'
              register: ubuntu_template_name
              changed_when: false

            - name: Download Ubuntu Template
              command: 'pveam download local {{ ubuntu_template_name.stdout }}'
              args:
                  creates: '/var/lib/vz/template/cache/{{ ubuntu_template_name.stdout }}'

            - name: Create Jellyfin LXC
              command: >
                  pct create {{ lxc.id }} /var/lib/vz/template/cache/{{ ubuntu_template_name.stdout }}
                  --hostname {{ lxc.hostname }}
                  --memory {{ lxc.memory }}
                  --swap {{ lxc.swap }}
                  --cores {{ lxc.cores }}
                  --ostype ubuntu
                  --net0 name=eth0,bridge=vmbr0,hwaddr=02:35:fa:19:7c:e8,ip={{ lxc.ip }}/24,gw={{ lxc.gateway }}
                  --nameserver "{{ env.JARVIS_DNS_SERVERS | join(' ') }}"
                  --storage {{ lxc.storage }}
                  --rootfs {{ lxc.storage }}:8
                  --unprivileged 1
                  --features keyctl=1,nesting=1
                  --tags media
                  --onboot 1

            - name: Start Jellyfin LXC
              command: pct start {{ lxc.id }}

            - name: Configure LXC Mounts and Passthrough
              blockinfile:
                  path: '/etc/pve/lxc/{{ lxc.id }}.conf'
                  marker: '# {mark} ANSIBLE MANAGED BLOCK'
                  block: |
                      lxc.cgroup2.devices.allow: c 226:* rwm
                      lxc.mount.entry: /dev/dri/renderD128 dev/dri/renderD128 none bind,optional,create=file
                      lxc.mount.entry: /dev/dri/card1 dev/dri/card1 none bind,optional,create=file
                      mp0: /mnt/wdhdd,mp=/media/wdhdd
                      mp1: /mnt/ssd,mp=/media/ssd
                      mp2: {{ paths.config }},mp=/etc/jellyfin
                      mp3: {{ paths.data }},mp=/var/lib/jellyfin
                      lxc.mount.entry: var/cache/transcodes var/lib/jellyfin/transcodes none bind,create=dir,optional 0 0
              register: lxc_config_update

            - name: Create Local Transcode Directory
              command: 'pct exec {{ lxc.id }} -- mkdir -p /var/cache/transcodes'
              changed_when: false

            - name: Restart Jellyfin LXC (Apply Mounts/Passthrough)
              command: pct reboot {{ lxc.id }}
              when: lxc_config_update.changed

            - name: Wait for LXC to be ready
              wait_for:
                  host: '{{ lxc.ip }}'
                  port: 22
                  timeout: 60
              delegate_to: localhost
              become: false
              ignore_errors: true

            - name: Enforce LXC Network Config (Persistence)
              command: >
                  pct set {{ lxc.id }}
                  --net0 name=eth0,bridge=vmbr0,hwaddr=02:35:fa:19:7c:e8,ip={{ lxc.ip }}/24,gw={{ lxc.gateway }}
                  --nameserver "{{ env.JARVIS_DNS_SERVERS | join(' ') }}"
              changed_when: false

            - name: Enforce LXC Network Config (Runtime)
              command: 'pct exec {{ lxc.id }} -- bash -c "ip route replace default via {{ lxc.gateway }} && echo \"nameserver {{ env.JARVIS_DNS_SERVERS | first }}\" > /etc/resolv.conf"'
              changed_when: false

            - name: Install Dependencies
              command: 'pct exec {{ lxc.id }} -- bash -c "export DEBIAN_FRONTEND=noninteractive && apt-get update && apt-get install -y curl gnupg"'
              register: apt_install
              retries: 20
              delay: 3
              until: apt_install.rc == 0
              changed_when: false

            - name: Install Jellyfin
              command: "pct exec {{ lxc.id }} -- bash -c 'curl -fsSL https://repo.jellyfin.org/install-debuntu.sh | bash'"
              changed_when: false

        when: not jellyfin_lxc_exists
