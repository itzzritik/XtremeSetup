# Home Assistant
home.jarvis.local {
  reverse_proxy http://host.docker.internal:8123
  tls {$JARVIS_USER_EMAIL}
}

# Code Server
code.jarvis.local {
  reverse_proxy http://code_server:8080
  tls {$JARVIS_USER_EMAIL}
}

# Redirect HTTP to HTTPS
http:// {
  redir https://{host}{uri}
}
